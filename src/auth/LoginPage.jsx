/**
 * LoginPage.jsx — Login Page Wrapper
 *
 * Route: /login
 * Access: PUBLIC
 *
 * Renders the LoginForm component for admin authentication.
 */
import React from 'react';
import { LoginForm } from './LoginForm';

export const LoginPage = () => {
    return <LoginForm />;
};
